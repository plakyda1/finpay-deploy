step "deploy-a-merchant-app" {
    name = "Deploy a merchant-app"
    properties = {
        Octopus.Action.TargetRoles = "merchants-k8s"
    }

    action {
        action_type = "Octopus.HelmChartUpgrade"
        properties = {
            Octopus.Action.GitRepository.Source = "External"
            Octopus.Action.Helm.ChartDirectory = "charts/merchant-portal"
            Octopus.Action.Helm.Namespace = "#{Namespace}"
            Octopus.Action.Helm.ReleaseName = "merchant-app-#{Octopus.Deployment.Tenant.Slug | ToLower}"
            Octopus.Action.Helm.ResetValues = "True"
            Octopus.Action.Helm.TemplateValuesSources = <<-EOT
                [
                  {
                    "Type": "Chart",
                    "ValuesFilePaths": "charts/merchant-portal/values.yaml\ncharts/merchant-portal/values-#{Octopus.Environment.Name | ToLower}.yaml"
                  },
                  {
                    "Type": "KeyValues",
                    "Value": {
                      "env.BRAND_COLOR": "#{BrandColor}",
                      "env.DATA_REGION": "#{DataRegion}"
                    }
                  }
                ]
                EOT
            Octopus.Action.Kubernetes.ResourceStatusCheck = "True"
            Octopus.Action.Script.ScriptSource = "GitRepository"
        }
        worker_pool = "hosted-ubuntu"

        git_dependencies {
            default_branch = "main"
            git_credential_id = "GitCredentials-23"
            git_credential_type = "Library"
            repository_uri = "#{ManifestsRepoUrl}"
        }
    }
}